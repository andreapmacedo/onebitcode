

// Objeto pode ser compreendido como um onjunto de elementos chave e valor
let spacialStation = {
    name: "Fox",
    platformsQuantity: 10,
    shape: "Esférica"
    
}

console.log(spacialStation.name.toUpperCase())


// Na lista, o acesso é sempre por meio de índice e não é necessário um atributo direto
let hitchedSpaceships = ["supernova", "Elemental", "Helmet"]
console.log(hitchedSpaceships[0].toUpperCase())